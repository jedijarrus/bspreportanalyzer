#!/bin/sh
# Letzte Prüfung vor dem Push: scannt ALLE getrackten Dateien (nicht nur den Diff).
echo "[pre-push] sanitize-check (alle getrackten Dateien) ..."
python scripts/sanitize_check.py --tracked || exit 1
